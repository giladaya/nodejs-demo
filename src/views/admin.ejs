<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Admin</title>
  </head>
  <body>
    <h1>Hello Admin</h1>

    <div>ID: <%= id %></div>
    <div>Name: <%= name %></div>
    <img src="<%= avatar %>" width="150px"/>

    <% if(admin) { %>
    <h2>Admin section</h2>
    <div>
      Admin stuffs
    </div>
    <% } %>
  </body>
</html>
